with Ada.Text_IO;
use Ada.Text_IO;

procedure Main is
   Count: Integer := 0;
begin
   for I in 1..5 loop
      Put_Line(A);
   end loop;

   while Count < 5 loop
      Put_Line(B);
      Count := Count + 1;
   end loop;
end Main;
