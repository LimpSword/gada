with Ada.Text_IO; use Ada.Text_IO;

procedure Test is
   function Mp(N,H: Integer;C: in out Character) return Integer is
      L : Integer;
      V : Character;
   begin
      x := 5;
      y := 3 + x;
      return y;
   end;
   X : Integer;
begin
   Put(X);
   New_Line;
end;
