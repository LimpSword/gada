    with Ada.Text_IO; use Ada.Text_IO;

    procedure Record4 is
        type List is record hi : Integer; end record;
        function f (x,z : Integer; y: boolean) return Integer is
        a : Integer := 8;
    begin
        if 0=1 then return 4;
            elsif 1=2 or 2=3 or 3=4 then return 3;
            else a:= 3;
            end if;
        return 2;
    end f;
    function f (x,y,z : Integer) return Boolean is
        a : Integer := 8;
    begin
         return true;
    end f;
        t : Integer;
        t1 : Boolean;
    begin
        t := f(1,2,true);
        t1 := f(1,2,3);
    end Record4;