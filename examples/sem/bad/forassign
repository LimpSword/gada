with Ada.Text_IO; use Ada.Text_IO;
procedure Test is
    y : Integer := 0;
begin
   for i in 0..10 loop
        y := y + (-1);
        Put(y);
        i:=i+1; -- can't assign loop variable
   end loop;
end Test;